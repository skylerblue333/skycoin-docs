{
  "name": "@skycoin4444/developer-hub-registry",
  "version": "0.1.0-beta.1",
  "description": "Deterministic SKYCOIN4444 developer resource registry core",
  "main": "dist/developerHub.js",
  "types": "dist/developerHub.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node tests/developerHub.test.js",
    "audit": "npm audit --omit=dev --audit-level=high",
    "verify": "npm run lint && npm test && npm run audit"
  },
  "keywords": ["skycoin4444", "developer", "docs", "registry"],
  "author": "SKYCOIN4444",
  "license": "MIT",
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
